# Bug Triagem — 2026-08-07

## Google Account (login SSO) indisponível no app — HIGH

**Reportado por:** @guilherme.topanotti (Slack DM, 07/ago)
**Descrição:** Usuário Android tentou conectar a conta via Google ontem (06/ago) e recebeu mensagem de que não era possível.
**Primeiro reporte?** SIM — nenhuma menção anterior em sessões, triagens locais ou issues do GitHub.

### Diagnóstico — regressão confirmada no código (restu-mobile)

Timeline do fluxo Google Sign-In:

| Data | Commit/PR | Mudança |
|---|---|---|
| 22-27/jul | PRs #57, #103, #107, #110, #114 | Fluxo OAuth (expo-auth-session) com múltiplos fixes |
| 29/jul | PR #119 (af2ebc3) | Migra para sheet nativo `@react-native-google-signin/google-signin@^16.1.4`; `utils/googleSignIn.js` como lazy loader (módulo real se existir, mock no Expo Go) |
| 29/jul | 9d74f1e | Lazy load seguro p/ Expo Go (try/catch no require) |
| **31/jul** | **8a45b2a** | **REVERTE p/ stub incondicional: remove a dependência do package.json, `getGoogleSignin()` sempre retorna mock, `isGoogleSignInAvailable()` sempre `false`** |
| 06/ago | (HEAD main) | Stub ainda presente |

### Causa raiz

- `restu-mobile/utils/googleSignIn.js:24` — `getGoogleSignin()` retorna `mockSignin` incondicionalmente
- `restu-mobile/utils/googleSignIn.js:39` — `isGoogleSignInAvailable()` retorna `false` sempre
- `restu-mobile/package.json` — dependência `@react-native-google-signin/google-signin` removida no commit 8a45b2a
- Efeito no `hooks/useGoogleSignIn.js`: tocar em "Continuar com Google" dispara `Alert` "Indisponível — O login com Google requer uma build nativa. Use o app via TestFlight ou dev client." → mensagem "não é possível" vista pelo usuário

### Classificação

- **Severidade:** HIGH (regressão de feature entregue + fluxo core de auth/cadastro)
- **Usuários afetados:** todos que tentam login via Google (Android e iOS) — sinalização de botão ativo porém bloqueado
- **Regressão:** SIM (funcionava em build nativa entre 29-31/jul; desabilitado silenciosamente em 31/jul)

### Dados não verificáveis nesta sessão

- **PostHog:** telemetria granular existe desde PR #119 (`sso_button_tapped`, `sso_google_step_*`, `sso_login_failed` c/ `reason`+`step`) — não acessível nesta sessão (chave indisponível). Consultar para dimensionar volume de `sso_button_tapped` vs `sso_login_failed reason=play_services_unavailable`.
- **Jira:** sem ticket criado ainda (aguardando confirmação de impacto).

### Ações recomendadas

1. Decidir caminho de distribuição: se o app é distribuído via build nativa (EAS/loja), **restaurar** `@react-native-google-signin/google-signin` no package.json + lazy loader (commit 9d74f1e). Se é Expo Go/OTA, **remover/ocultar o botão Google** até existir build nativa.
2. Criar ticket Jira SWNPGMO (auth/google) com esta triagem.
3. Instrumentar: conferir PostHog `sso_button_tapped` (quantos tocam vs conseguem) para dimensionar impacto real.
4. Após fix, validar em device Android real com Play Services (não basta emulador) — SHA-1 do keystore precisa estar registrado no Google Cloud Console (requisito do sheet nativo).

### Arquivos relevantes

- `restu-mobile/utils/googleSignIn.js:24,39` (stub incondicional)
- `restu-mobile/hooks/useGoogleSignIn.js` (alerta "Indisponível")
- `restu-mobile/package.json` (dependência removida em 8a45b2a)
- `restu-web/src/app/api/auth/me/route.ts` (PR #208, 06/ago — validação UUID; risco secundário, revisar se userId de SSO é UUID)
