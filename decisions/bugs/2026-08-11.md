# Bug: Notificação abre review sem ações (like/comment/save/follow) — RE-REPORT

**Data:** 2026-08-11
**Reportado por:** @bruno.parizotto (re-report — mesmo reportante da triagem original)
**Severidade:** HIGH (duplicata, sem fix mergeado)
**Referência:** triagem original `bugs/2026-08-02.md`

## Descrição (re-report)

Reaberto pelo mesmo reportante: ao acessar um post via "área de notificações":
1. Impossível comentar ou reagir
2. Bookmark ("salvar") não funciona
3. Opção "seguir" aparece mesmo para usuário já seguido

Sintomas idênticos à triagem de 02/ago — sem divergência nova.

## Status do fix (verificado em 11/ago via GitHub)

- ❌ Nenhum PR mergeado corrigindo `GET /api/reviews/[id]` (restu-web)
  - Último commit em `src/app/api/reviews/[id]/route.ts`: `1de7d59` (05/ago, migração pino logger — não é fix)
- ❌ Nenhum issue aberto no GitHub (restu-web/restu-mobile)
- ⚠️ PR #172 (restu-web, OPEN 03/ago): "remove reações não implementadas — só ❤️" — mexe em `ReviewActions.js` mas NÃO cobre os campos ausentes do detail endpoint (`type`, `saved`, `user.isFollowing`)
- ⚠️ PR #131 (restu-mobile, OPEN 04/ago): highlightCommentId sticky — bug diferente

## Conclusão

Causa raiz segue aberta (11 dias): `GET /api/reviews/[id]` omite viewer-state que o feed inclui.
Ação: escalar fix do backend (enriquecer route.ts) + desbloquear PR #172 se relacionado.
